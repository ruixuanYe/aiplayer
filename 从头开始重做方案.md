# AIPlayer Companion 从头开始重做方案

## 当前决定

当前已实装到游戏里的旧版 `aiplayer-companion` 已从 PCL 的 mods 目录移除并备份。

新版不再继续沿用旧代码的“玩家 Bot + 粗糙坐标追踪”路线。  
后续从一个干净架构重新开始。

## 核心目标

做一个真正可长期迭代的 Minecraft AI 玩家伙伴。

优先级：

1. 实体基础必须稳定。
2. 碰撞、受击、击退、伤害必须尽量走原版玩家逻辑。
3. 巡路不能靠贴脸追踪，必须有明确路径规划。
4. AI 不能直接执行任意代码或命令。
5. LM Studio 只负责理解意图和决策，不直接接管底层危险操作。

## 不再沿用的问题方案

旧方案的问题：

- 服务端假玩家虽然继承 `ServerPlayerEntity`，但控制层过于粗糙。
- 移动大量依赖手写坐标追踪，容易浮空、卡墙、贴脸、乱跳。
- 受击、击退、武器伤害和真人玩家仍有差距。
- 巡路、战斗、拾取、装备逻辑耦合在一起，越修越乱。
- 右键/G 键菜单、聊天、行为控制和 AI 决策边界不够清晰。

因此新版不在旧结构上继续打补丁。

## 新版总体架构

新版分为五层：

1. Bot 实体层
   - 目标是实现 Carpet 假人风格的服务端假玩家。
   - 优先研究 Carpet fake player 的实际实现方式。
   - 不能只使用 Fabric API 默认 `FakePlayer`，因为 Fabric 默认 FakePlayer 更偏向工具人，不适合作为生存伙伴。

2. 输入控制层
   - 只暴露白名单动作。
   - 例如：前进、后退、左移、右移、跳跃、潜行、疾跑、攻击、使用物品、切换快捷栏、丢物品。
   - 不允许模型执行 Java 代码、系统命令、脚本。

3. 路径规划层
   - 参考 Baritone 思路重写服务端路径规划。
   - 不直接照搬 Baritone 源码。
   - 路径规划要支持：
     - 平地行走
     - 上一格台阶
     - 跳跃
     - 掉坑脱困
     - 开门
     - 梯子
     - 安全传送兜底

4. 行为层
   - 跟随主人
   - 等待
   - 保护主人
   - 主动攻击
   - 自动拾取
   - 自动装备
   - 背包查询和给物品

5. AI 决策层
   - LM Studio 负责根据玩家聊天判断意图。
   - 模型输出只能是结构化动作请求。
   - 服务端根据白名单决定是否执行。

## 第一阶段目标

版本建议：`2.0.0-clean`

只做最小稳定底座：

- 删除旧 AIPlayerEntity/NPC 路线。
- 删除旧粗糙 Bot 控制器。
- 重新建立项目结构。
- 实现一个干净的服务端假玩家生成和删除。
- 确认：
  - 只生成一个 Bot。
  - Bot 是玩家列表中的玩家实体。
  - Bot 有正常碰撞箱。
  - Bot 能被攻击。
  - Bot 死亡后不会残留两个。
  - Bot 不会默认飞行。
  - Bot 先不做复杂巡路。

第一阶段不要急着加 AI 聊天、自动装备、战斗、菜单。

## 第二阶段目标

版本建议：`2.1.0-pathing`

实现路径规划底座：

- 服务端 A* 节点搜索。
- 节点类型：
  - WALK
  - STEP_UP
  - DROP_DOWN
  - JUMP
  - OPEN_DOOR
  - CLIMB_LADDER
  - TELEPORT_FALLBACK
- 路径执行器只负责把路径节点转换成白名单输入动作。
- 跟随主人先只支持地面生存跟随。
- 主人创造飞行时，Bot 不飞，只在地面追随或提示无法跟随。

## 第三阶段目标

版本建议：`2.2.0-combat-inventory`

实现生存行为：

- 自动拾取附近掉落物。
- 自动装备更好的武器和盔甲。
- 攻击模式：
  - 主动攻击主人范围内敌对生物。
  - 攻击对主人有仇恨的非敌对生物。
- 保护模式：
  - 只攻击正在攻击主人或以主人为目标的生物。
- 背包查询：
  - 玩家问“你背包有什么”时，Bot 汇报背包内容。
- 给物品：
  - 玩家问“把 xxx 给我”时，Bot 从背包丢出对应物品。

## 第四阶段目标

版本建议：`2.3.0-ai-tools`

接入 LM Studio 工具调用：

- 模型只能输出 JSON 工具调用。
- 工具白名单：
  - follow_owner
  - wait_here
  - come_to_owner
  - inspect_inventory
  - drop_item
  - equip_best_weapon
  - equip_best_armor
  - enable_combat
  - disable_combat
  - protect_owner
- 服务端验证权限和参数。
- 模型不能直接操作 Java 方法。

## 第五阶段目标

版本建议：`2.4.0-ui`

重新做 G 键菜单：

- 一级菜单：
  - AIPlayer
  - 行为模式
  - 战斗选项
  - 装备与背包
  - AI 设置
- 菜单必须可滚动。
- 不使用右键打开主菜单。
- 右键只保留轻量交互或完全关闭。

## 当前禁止事项

新版开始前暂时不要做：

- 皮肤系统
- 建造
- 挖矿
- 长期记忆
- 自由执行命令
- 双开真实客户端
- 直接接入完整 Baritone 客户端

## 下一步

下一步先做 `2.0.0-clean`：

1. 建新分支。
2. 备份旧源码状态。
3. 删除旧混乱实现。
4. 保留 Gradle/Fabric 基础工程。
5. 重新实现最小服务端假玩家。
6. 构建 jar。
7. 装入游戏测试基础碰撞、受击、死亡、唯一生成。
